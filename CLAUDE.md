# Project Guidelines

## Git Workflow

- After major changes, create a new branch, commit, and push to the remote repo.
- Branch naming: `feature/<description>` or `fix/<description>`.
- Always push new branches with `git push -u origin <branch-name>`.
